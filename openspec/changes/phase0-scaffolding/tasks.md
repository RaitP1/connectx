## 1. Solution and Project Creation

- [ ] 1.1 Create solution file: `dotnet new sln -n ConnectX`
- [ ] 1.2 Create Domain class library: `dotnet new classlib -n Domain -o src/Domain`
- [ ] 1.3 Create Application class library: `dotnet new classlib -n Application -o src/Application`
- [ ] 1.4 Create Infrastructure class library: `dotnet new classlib -n Infrastructure -o src/Infrastructure`
- [ ] 1.5 Create ConsoleApp console project: `dotnet new console -n ConsoleApp -o src/ConsoleApp`
- [ ] 1.6 Create ConnectX.Tests xUnit project: `dotnet new xunit -n ConnectX.Tests -o tests/ConnectX.Tests`
- [ ] 1.7 Add all five projects to the solution

## 2. Shared Build Configuration

- [ ] 2.1 Create `Directory.Build.props` at repo root with TargetFramework net9.0, Nullable enable, TreatWarningsAsErrors true, ImplicitUsings enable
- [ ] 2.2 Remove duplicated TargetFramework/Nullable/ImplicitUsings from individual `.csproj` files (generated by `dotnet new`)

## 3. Project References

- [ ] 3.1 Add Application -> Domain reference
- [ ] 3.2 Add Infrastructure -> Application reference
- [ ] 3.3 Add ConsoleApp -> Application + Infrastructure references
- [ ] 3.4 Add ConnectX.Tests -> Domain + Application + Infrastructure references

## 4. NuGet Packages

- [ ] 4.1 Add `Microsoft.EntityFrameworkCore.Sqlite` and `Microsoft.EntityFrameworkCore.Design` to Infrastructure
- [ ] 4.2 Add `Microsoft.EntityFrameworkCore.Sqlite` to ConsoleApp (for migrations CLI)
- [ ] 4.3 Verify ConnectX.Tests has `xunit` and `Microsoft.NET.Test.Sdk` packages (from template)

## 5. Architecture Tests

- [ ] 5.1 Write `ArchitectureTests.cs` in ConnectX.Tests with three tests: Domain has no ConnectX project refs, Application refs only Domain, Infrastructure does not ref ConsoleApp or WebApp
- [ ] 5.2 Remove template-generated test file (e.g., `UnitTest1.cs`) from ConnectX.Tests

## 6. Git and Build Verification

- [ ] 6.1 Generate `.gitignore` via `dotnet new gitignore`
- [ ] 6.2 Run `dotnet build` — verify zero errors
- [ ] 6.3 Run `dotnet test` — verify all 3 architecture tests pass
