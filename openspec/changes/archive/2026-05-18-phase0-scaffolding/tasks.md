## 1. Solution and Project Creation

- [x] 1.1 Create solution file: `dotnet new sln -n ConnectX`
- [x] 1.2 Create Domain class library: `dotnet new classlib -n Domain -o src/Domain`
- [x] 1.3 Create Application class library: `dotnet new classlib -n Application -o src/Application`
- [x] 1.4 Create Infrastructure class library: `dotnet new classlib -n Infrastructure -o src/Infrastructure`
- [x] 1.5 Create ConsoleApp console project: `dotnet new console -n ConsoleApp -o src/ConsoleApp`
- [x] 1.6 Create ConnectX.Tests xUnit project: `dotnet new xunit -n ConnectX.Tests -o tests/ConnectX.Tests`
- [x] 1.7 Add all five projects to the solution

## 2. Shared Build Configuration

- [x] 2.1 Create `Directory.Build.props` at repo root with TargetFramework net9.0, Nullable enable, TreatWarningsAsErrors true, ImplicitUsings enable
- [x] 2.2 Remove duplicated TargetFramework/Nullable/ImplicitUsings from individual `.csproj` files (generated by `dotnet new`)

## 3. Project References

- [x] 3.1 Add Application -> Domain reference
- [x] 3.2 Add Infrastructure -> Application reference
- [x] 3.3 Add ConsoleApp -> Application + Infrastructure references
- [x] 3.4 Add ConnectX.Tests -> Domain + Application + Infrastructure references

## 4. NuGet Packages

- [x] 4.1 Add `Microsoft.EntityFrameworkCore.Sqlite` and `Microsoft.EntityFrameworkCore.Design` to Infrastructure
- [x] 4.2 Add `Microsoft.EntityFrameworkCore.Sqlite` to ConsoleApp (for migrations CLI)
- [x] 4.3 Verify ConnectX.Tests has `xunit` and `Microsoft.NET.Test.Sdk` packages (from template)

## 5. Architecture Tests

- [x] 5.1 Write `ArchitectureTests.cs` in ConnectX.Tests with three tests: Domain has no ConnectX project refs, Application refs only Domain, Infrastructure does not ref ConsoleApp or WebApp
- [x] 5.2 Remove template-generated test file (e.g., `UnitTest1.cs`) from ConnectX.Tests

## 6. Git and Build Verification

- [x] 6.1 Generate `.gitignore` via `dotnet new gitignore`
- [x] 6.2 Run `dotnet build` — verify zero errors
- [x] 6.3 Run `dotnet test` — verify all 3 architecture tests pass
